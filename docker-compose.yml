services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=voting.db
      # You can pass your Gemini API key from the host
      - GEMINI_API_KEY=AIzaSyADeTX2yuyyhwVZmFSwHKjC6QzLWTej-sw

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
